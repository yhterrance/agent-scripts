interface:
  display_name: "Delegate Work"
  short_description: "Delegate work while keeping the main thread responsive"
  default_prompt: "Use $delegate-work to coordinate this substantive task with focused subagents."
